# tree-sitter-apacheconf

[Apache HTTP Server](https://httpd.apache.org/) conf grammar for [tree-sitter](https://tree-sitter.github.io/tree-sitter/).

## Limitions

* it does not check open & close tag matches